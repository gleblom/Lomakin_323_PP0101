<UserControl x:Class="DocumentManagementService.ViewModels.SecurityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DocumentManagementService.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{DynamicResource {x:Static SystemColors.MenuBrushKey}}">
        <StackPanel>
            <TextBlock  FontSize="18" FontWeight="Bold" Text="Пароль"/>
            <Button
                        Margin="0,10,0,10"
                        Visibility="{Binding ButtonVisibility}" 
                        Content="Сменить пароль" Width="284" Background="{x:Null}" BorderBrush="{x:Null}"/>
        </StackPanel>
        <StackPanel Visibility="{Binding PanelVisibility}">
            <TextBlock Text="Старый пароль" FontSize="18" FontWeight="Bold"/>
            <TextBox Margin="10" Text="{Binding Telephone}" 
                     Style="{StaticResource MaterialDesignFilledTextBox}"
                     Height="32"/>
            <TextBlock  Text="Новый пароль" FontSize="18" FontWeight="Bold"/>
            <TextBox Margin="10" Text="{Binding Position}" 
                     Style="{StaticResource MaterialDesignFilledTextBox}"
                     Height="32"/>
            <TextBlock Text="Подтверждение нового пароля" 
                           FontSize="18" FontWeight="Bold"/>
            <TextBox Margin="10" Text="{Binding Position}" 
                     Style="{StaticResource MaterialDesignFilledTextBox}"
                     Height="32"/>
            <Button Margin="10,10,10,10" Content="Обновить пароль" BorderBrush="{x:Null}" Background="{x:Null}"/>
        </StackPanel>
    </Grid>
</UserControl>
